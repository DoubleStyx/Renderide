# Renderide renderer configuration (INI).
# Missing keys and commented lines use built-in defaults.
# Path resolution: see crate::config module docs (RENDERIDE_CONFIG, exe dir, cwd, ...).
#
# Example sections for future options (all commented):
#
# [display]
# focused_fps = 240
# unfocused_fps = 60
#
# [rendering]
# vsync = true
#
# [debug]
# log_verbose = false
